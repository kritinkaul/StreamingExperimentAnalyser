# Data Directory

This directory contains the experiment results.

- `experiment_results.json` - Pre-computed analysis results for demo
- `streaming.duckdb` - Local database (not included in repo, too large)

To run the full pipeline locally, follow the setup instructions in the main README.
